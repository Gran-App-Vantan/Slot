services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/workdir
    ports:
      - 8080:8000
  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/workdir
    ports:
      - 3005:3000
  db:
    image: mysql:8.3
    volumes:
      - ./backend/mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: dev
    ports:
      - 3680:3306